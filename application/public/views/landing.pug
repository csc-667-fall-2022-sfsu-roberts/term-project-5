extends /layouts/layout

block content    
  nav#navbar
    h2 Welcome #{username}
    ul#navBarUL
      if logged 
        li
          a#signout-button Sign Out
      else
        li
          a(href='/auth/signup') Sign Up
      li
        a(href='/lobby') Lobby page
      li
        a(href='/games/1') Game page   